apiVersion: v1
kind: Service
metadata:
  name: java-devops-service
spec:
  type: NodePort
  selector:
    app: java-devops-automation-project
  ports:
  - port: 9090
    targetPort: 9090
    nodePort: 30007
