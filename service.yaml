apiVersion: v1
kind: Service
metadata:
  name: devops-automation-project-service
spec:
  type: NodePort
  selector:
    app: devops-automation-project-app
  ports:
  - port: 9090
    targetPort: 9090
    nodePort: 30007
